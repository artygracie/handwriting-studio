# Model (otuva/handwriting-synthesis).
# Apple Silicon needs the native arm64 build: Intel TF wheels require AVX, which
# Rosetta does not emulate (causes "Abort trap: 6"). Use tensorflow-macos on
# arm64 macOS, plain tensorflow everywhere else.
tensorflow-macos==2.12.0; sys_platform == "darwin" and platform_machine == "arm64"
tensorflow==2.12.0; sys_platform == "darwin" and platform_machine == "x86_64"
tensorflow==2.12.0; sys_platform == "linux"
tensorflow==2.12.0; sys_platform == "win32"
tensorflow-probability==0.20.1
# numpy must stay <1.24: the model uses ndarray.tostring(), removed in 1.24.
numpy==1.23.5
scipy==1.10.1
scikit-learn>=1.0
pandas>=1.3
matplotlib>=3.3
svgwrite>=1.4

# Web app
fastapi>=0.100
uvicorn>=0.22
pydantic>=2.0
